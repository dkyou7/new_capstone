<div class="container">
    <h1>Post <%=@post.id%></h1>
    <form action="/index">
      <div class="form-group">
        <label for="title">제목 : </label>
        <%=@post.title%>
      </div>
      <div class="form-group">
        <label for="content">내용 : </label>
        <%=@post.content%>
        </p> 
      </div>
    <input type="submit" value="뒤로가기">
    </form>
</div>